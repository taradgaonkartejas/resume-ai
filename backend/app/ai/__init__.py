"""Multi-agent layer: model gateway, retrieval, agents and LangGraph graphs.

Every component degrades to a deterministic fallback when no API key is
configured, so the full product works offline.
"""
